const circle = require('./circle');
// import circle from 'circle';

const radius = 3;
console.log(`The circumference of a circle of radius ${radius} is ${circle.circumference(radius)}`) // шаблонные строки
console.log(`Its area is ${circle.area(radius)}`)
